# Adversarial audit

A short document of the things that tend to break TTS.

## Numbers

The population was 1,200,000 in 2026, up from 42,000 in 2010. Pi is 3.14159.
The temperature dropped to -5 degrees. The score was 12-3.

## Dates and times

Meeting on 2026-05-17 at 3:30pm. Other formats: 05/17/2026, May 17 2026.
Office hours 9-11am.

## URLs and paths

Long URL: https://example.com/users/42?ref=audit&token=abc. File at
/Users/alex/projects/notes.md. Repo github.com/openai/openai-python.

## Emails

Plain alex@example.com, dotted first.last@example.com, plus-alias
alex+newsletter@example.com.

## Acronyms

NASA launched the API for SQL queries. The CEO of NVIDIA spoke at NeurIPS.
NATO and the UN agreed. HTTP and HTTPS differ.

## Versions and flags

Install Python 3.10+ or use uv 0.4.21. Run `git commit --no-verify`. Use
`pip install -e '.[local]'`. Version v3.14.2 shipped.

## Code with operators

The condition `x != 0 && y >= 10` holds. Set `count += 1` in the loop. The
list `[1, 2, 3]` has length `len(xs) == 3`.

## Punctuation and unicode

Em-dash example — like this. En-dash range 10–20. Smart quotes "hello" and
'world'. Ellipsis… and the arrow → and the bullet •.

## Hyphenation

A state-of-the-art co-author wrote a well-known peer-reviewed paper. The
re-entry was rock-solid.

## Phone

Call (555) 123-4567 or +1-800-555-0199 for support.

## Equations the rule reader can't fully handle

A path integral: $\int \mathcal{D}\phi \, e^{iS[\phi]/\hbar}$. A tensor:
$T^{\mu\nu}_{\;\;\rho\sigma}$. A commutator: $[\hat{x}, \hat{p}] = i\hbar$.
